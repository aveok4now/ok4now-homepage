---
import { GH_LINK, SKIES_4NOW_LINK } from "../config";
import TypeWriter from "./TypeWriter";
import { getLyrics } from "../pages/api/ok4now-genius";

const today = new Date();
const lyrics = await getLyrics();
---

<footer class="text-zinc-500 p-4 text-center">
	&copy; {today.getFullYear()}
	<a href={GH_LINK}>Slava Ch</a>. All rights reserved.

	<a
		href={SKIES_4NOW_LINK}
		class="flex justify-center items-start overflow-hidden h-10 my-1"
	>
		<code
			class="leading-tight text-xs hover:text-black dark:hover:text-white transition-colors duration-700"
		>
			<TypeWriter
				words={lyrics}
				loop={false}
				typeSpeed={100}
				deleteSpeed={25}
				client:visible
			/>
		</code>
	</a>
</footer>
