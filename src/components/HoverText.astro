---
interface Props {
	text: string;
	className?: string;
}

const { text, className } = Astro.props;
---

<span
	class={` hover:text-black dark:hover:text-white transition-colors duration-700 ${className} cursor-pointer`}
	>{text}</span
>
